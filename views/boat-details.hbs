<h1>{{vehiclename}}</h1>
  <div class="car-detail-container">
    <img src="{{picture}}" alt="car-picture" />
    <div class="specification">
      <h2>Vehicle Specification</h2>
      <p><span class="float-left">Size:</span><span
          class="float-right"
        >{{size}}</span></p>
      <p><span class="float-left">PASSENGERS:</span><span
          class="float-right"
        >{{passengers}}</span></p>

    </div>
  </div>

   {{!--  <a href='/cars/{{_id}}'>See details</a> --}}
      <form action='/search-boats' method='post'>
      <div class="availability-container">
      <label for='start-date' class="margin-top">Start Date</label>
      <input type='date' name='startDate' />

      <label for='end-date' class="margin-top">End Date</label>
      <input type='date' name='endDate' />

      <input type="text" name="vehiclename" value="{{vehiclename}}" hidden>
      <input type="number" name="price" value="{{price}}" hidden>
      <input type="number" name="passengers" value="{{passangers}}" hidden>
      <input type="text" name="size" value="{{size}}" hidden>

      <button type='submit' class="price-button margin-top">Search availability</button>

    {{!-- date --}}
    {{!-- vehicle input: value = vehiclename --}}
    </div>
   </form>
  </div>

  {{#if errorMessage}}
  <p>{{errorMessage}}</p>
  {{/if}}